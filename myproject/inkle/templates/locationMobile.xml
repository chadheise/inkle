<?xml version="1.0" encoding="UTF-8" ?>
<location_inklings>
    <dinner number="{{member.dinner_members|length}}">
        {% for m in member.dinner_members %}
            <member>
                <name>{{ m.get_full_name }}</name>
                <id>1</id>
            </member>
        {% endfor %}
        {% if member.dinner_members %}
            <message>There {% if member.num_dinner_others == 1 %}is{% else %}are{% endif %} {{ member.num_dinner_others }} other member{{ member.num_dinner_others|pluralize }} attending this inkling.</message>
        {% else %}
            <message>There {% if member.num_dinner_others == 1 %}is{% else %}are{% endif %} {{ member.num_dinner_others }} member{{ member.num_dinner_others|pluralize }} attending this inkling.</message>
        {% endif %}
    </dinner>

    <pregame number="{{member.pregame_members|length}}">
        {% for m in member.pregame_members %}
            <member>
                <name>{{ m.get_full_name }}</name>
                <id>1</id>
            </member>
        {% endfor %}
        {% if member.pregame_members %}
            <message>There {% if member.num_pregame_others == 1 %}is{% else %}are{% endif %} {{ member.num_pregame_others }} other member{{ member.num_pregame_others|pluralize }} attending this inkling.</message>
        {% else %}
            <message>There {% if member.num_pregame_others == 1 %}is{% else %}are{% endif %} {{ member.num_pregame_others }} member{{ member.num_pregame_others|pluralize }} attending this inkling.</message>
        {% endif %}
    </pregame>

    <main_event number="{{member.main_event_members|length}}">
        {% for m in member.main_event_members %}
            <member>
                <name>{{ m.get_full_name }}</name>
                <id>1</id>
            </member>
        {% endfor %}
        {% if member.main_event_members %}
            <message>There {% if member.num_main_event_others == 1 %}is{% else %}are{% endif %} {{ member.num_main_event_others }} other member{{ member.num_main_event_others|pluralize }} attending this inkling.</message>
        {% else %}
            <message>There {% if member.num_main_event_others == 1 %}is{% else %}are{% endif %} {{ member.num_main_event_others }} member{{ member.num_main_event_others|pluralize }} attending this inkling.</message>
        {% endif %}
    </main_event>
</location_inklings>