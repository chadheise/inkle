<?xml version="1.0" encoding="UTF-8" ?>

{% if locations %}
    <locations number={{locations|length}}>
    {% for location in locations %}
        <location>
            <count>{{ location.count }}</count>
            {% if location.name %}
                <name>{{ location.name}}</name>
            {% else %}
                <name>{{ location.get_full_name }}'s Place</name>
            {% endif %}
            {% if location.street %}
                <street>{{ location.street }}</street>
            {% endif %}
            {% if location.city and location.state %}
                <citystate>{{ location.city }}, {{ location.state }}</citystate>
            {% endif %}
            <id>192</id>
            {% if location.name %}
                <type>location</type>
            {% else %}
                <type>memberPlace</type>
            {% endif %}
        </location>
    {% endfor %}
    </locations>
{% else %}
    <locations number=0>
    </locations>
{% endif %}
